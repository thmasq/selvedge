[package]
name = "selvedge-shell"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
gloo-worker = { workspace = true }
leptos = { workspace = true }
selvedge-shared = { path = "../shared" }
serde = { workspace = true }
wasm-bindgen = { workspace = true }
